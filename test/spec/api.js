'use strict';

const should = require('should');
const api = require('../../lib/api');

describe('api', () => {
    describe('getCommitDates', () => {
        xit('should be gather commits for the specified year', () => {
            return api.getCommitDates({

            });
        });
    });
});
